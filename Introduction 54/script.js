alert("Hello World");

// whhat is alert
// alert() is a method used to display a pop-up message to the user. It is commonly used for providing information or warnings. When called, it creates a modal dialog box with the specified message and an "OK" button for the user to dismiss it.

console.log("Code is running..")
console.log("Code is also running..")
console.log("Code is looking like a wow..")

// what is console log 
// console.log() is a method used to output messages to the browser's console for debugging purposes.

var a = prompt("Enter Your number")
var isTrue = confirm("Are you Sure you wanted to leave this page and blast your Computer")

// what is prompt
// prompt() is a method used to display a dialog box that prompts the user for input. It takes a string argument that serves as the message displayed to the user, and it returns the input entered by the user as a string.

 
// what is confirm
// confirm() is a method used to display a dialog box with a message and two buttons: "OK" and "Cancel". It returns true if the user clicks "OK" and false if the user clicks "Cancel".

if(isTrue){
    console.log("Computer is blasting..")
}
else{
    console.log("Computer is Not blasting..")
}
console.log("Your number is " + a)

document.title = "This is Dovieee"
// document.body.style.backgroundColor = "red"
