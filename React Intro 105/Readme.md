# Why React?

 + States can be used if we update a state it updates the changes across the pages

 + App Splits into multiple components so it can be reused 

 + Performance Better react uses virtual DOM ijstead of DOM Manipulation to update UI

 + Debugging and Easy Maintainance
 